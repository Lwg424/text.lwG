#include<stdio.h>
main()
{
    int n,m,a[100],b[100];
    int i,j,num,t;
    scanf("%d %d",&n,&m);
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    for(i=0;i<m;i++)
    {
        scanf("%d",&b[i]);
    }
    for(i=n,j=0;j<m;i++,j++)
    {
        a[i]=b[j];
    }
    n=n+m;
    for(i=0,num=0;i<n-1-num;i++)
    {
        for(j=i+1;j<n;j++)
        {
            if(a[i]==a[j])
            {
                for(;j<n;j++)
                {
                    a[j]=a[j+1];
                }
                b[num]=a[i];
                num++;
                j=i;
            }
        }
    }
    for(i=0;i<n-num;i++)
    {
        printf("%d ",a[i]);
    }
    printf("\n");
    n=num;
    for(i=0,num=0;i<n-1-num;i++)
    {
        for(j=i+1;j<n;j++)
        {
            if(b[i]==b[j])
            {
                for(;j<n;j++)
                {
                    b[j]=b[j+1];
                }
                num++;
                j=i;
            }
        }
    }
    for(j=0;j<n-num;j++)
    {
        printf("%d ",b[j]);
    }
}
